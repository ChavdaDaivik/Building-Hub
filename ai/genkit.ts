// Optional AI integration stubbed to avoid missing dependency build errors
export const ai = {
  generate: async (_prompt: string) => {
    return { text: 'AI not configured in this build.' };
  },
};
